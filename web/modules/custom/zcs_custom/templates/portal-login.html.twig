

<div class="custom-content">
  {# <h1>{{ title }}</h1> #}
    <div class="text-center">
			<h1>{{ description }}</h1>
			<a href="{{ path('user.login') }}" class="btn btn-primary">{{ 'Login'|t }}</a>
    </div>
</div>